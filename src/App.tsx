import React from 'react';
import Tournament from './Tournament';

const App: React.FC = () => {
    return (
        <div className="App">
            <Tournament />
        </div>
    );
};

export default App;
